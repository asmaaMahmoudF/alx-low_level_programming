#include "lists.h"
/**
  * print_listint_safe - inserts a new node at a given position
  * @head: head of the list
  * Return: a pointer to the first node of the reversed list
  */
size_t print_listint_safe(const listint_t *head)
{
	return (0);
}
