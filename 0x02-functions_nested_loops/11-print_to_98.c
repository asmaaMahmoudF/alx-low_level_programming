#include "main.h"
/**
 * Description: print_to_98 - prints n to 98 counts
 * separated by comma, followed by space and number
 * should be printed in oreder
 * @n: input
 * Return void
*/
void print_to_98(int n)
{
int sum;
if (sum > 98)
{
for (sum = n ; sum > 98 ; sum--)
{
printf("%d, ", count);
}
}
else
{
for (sum = n ; sum < 98 ; sum++)
{
printf("%d, ", count);
}
}
print("98\n");
}
